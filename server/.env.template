# Production Environment Variables Template for Server
# Copy this file and rename to .env
# Update the values with your actual MongoDB Atlas and other credentials

# MongoDB Atlas Connection String
MONGO_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database-name?retryWrites=true&w=majority

# JWT Secret Key (Generate a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port (Railway will set this automatically)
PORT=5000

# Environment
NODE_ENV=production

# CORS Origin (Your Vercel frontend URL)
CORS_ORIGIN=https://your-vercel-app.vercel.app

# Optional: Email service credentials for password reset
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password